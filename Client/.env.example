# API Configuration
# The base URL for the backend API
# Make sure the backend server is running on this URL
# and has a valid GitHub token configured
VITE_API_URL=http://localhost:3000

# Vite HMR (Hot Module Replacement) Configuration
# Host for Vite's development server hot reload
VITE_HMR_HOST=localhost
